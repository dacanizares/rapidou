interface:
  display_name: "Rapidou Code Review"
  short_description: "Review changes for behavior and simplicity"
  default_prompt: "Use $code-review to inspect the current diff against the spec and repository rules."
